web: bundle exec rdbg --open --nonstop --command -- bundle exec rails server -p 3000 -b '0.0.0.0'
js: REBUILD_VUE=false yarn build --watch
worker: bundle exec sidekiq
# Mailcatcher is started as a daemon by bin/dev.
vite: bin/vite dev
